# gherkin-test-facilitator

Execute gherkin-test-facilitator prompt

## Usage

This command invokes the `gherkin-test-facilitator` prompt using the prompt CLI.

```bash
prompt gherkin-test-facilitator
```

## Execution

When you invoke this command (e.g., `/gherkin-test-facilitator`), Claude Code will execute the full prompt workflow.

---

**Note**: This command is auto-generated from `~/git/dotfiles/prompts/gherkin-test-facilitator.md`. To update, modify the source prompt file and re-run `ax claude sync-prompts-to-commands`.

---

## Instructions

prompt gherkin-test-facilitator
