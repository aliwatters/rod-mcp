# grooming-cycle

Execute grooming cycle orchestrator - autonomous issue grooming workflow

## Usage

This command invokes the `grooming-cycle` prompt using the prompt CLI.

```bash
prompt grooming-cycle
```

## Execution

When you invoke this command (e.g., `/grooming-cycle`), Claude Code will execute the full prompt workflow.

---

**Note**: This command is auto-generated from `~/git/dotfiles/prompts/grooming-cycle.md`. To update, modify the source prompt file and re-run `ax claude sync-prompts-to-commands`.

---

## Instructions

prompt grooming-cycle
