# verify-pr

Standalone PR verification command for gathering feedback, classifying comments, and preparing responses

## Usage

This command invokes the `verify-pr` prompt using the prompt CLI.

```bash
prompt verify-pr
```

## Execution

When you invoke this command (e.g., `/verify-pr`), Claude Code will execute the full prompt workflow.

---

**Note**: This command is auto-generated from `~/git/dotfiles/prompts/verify-pr.md`. To update, modify the source prompt file and re-run `ax claude sync-prompts-to-commands`.

---

## Instructions

prompt verify-pr
