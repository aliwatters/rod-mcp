# gherkin-refinement

Execute gherkin-refinement prompt

## Usage

This command invokes the `gherkin-refinement` prompt using the prompt CLI.

```bash
prompt gherkin-refinement
```

## Execution

When you invoke this command (e.g., `/gherkin-refinement`), Claude Code will execute the full prompt workflow.

---

**Note**: This command is auto-generated from `~/git/dotfiles/prompts/gherkin-refinement.md`. To update, modify the source prompt file and re-run `ax claude sync-prompts-to-commands`.

---

## Instructions

prompt gherkin-refinement
