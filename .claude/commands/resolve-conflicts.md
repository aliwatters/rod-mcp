# resolve-conflicts

Resolve merge conflicts in pull requests systematically.

## Usage

This command invokes the `resolve-conflicts` prompt using the prompt CLI.

```bash
prompt resolve-conflicts
```

## Execution

When you invoke this command (e.g., `/resolve-conflicts`), Claude Code will execute the full prompt workflow.

---

**Note**: This command is auto-generated from `~/git/dotfiles/prompts/resolve-conflicts.md`. To update, modify the source prompt file and re-run `ax claude sync-prompts-to-commands`.

---

## Instructions

prompt resolve-conflicts
