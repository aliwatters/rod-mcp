# pr-ready-cycle

Execute PR-ready cycle orchestrator - automated PR preparation workflow

## Usage

This command invokes the `pr-ready-cycle` prompt using the prompt CLI.

```bash
prompt pr-ready-cycle
```

## Execution

When you invoke this command (e.g., `/pr-ready-cycle`), Claude Code will execute the full prompt workflow.

---

**Note**: This command is auto-generated from `~/git/dotfiles/prompts/pr-ready-cycle.md`. To update, modify the source prompt file and re-run `ax claude sync-prompts-to-commands`.

---

## Instructions

prompt pr-ready-cycle
