# mcp-server-test

Execute mcp-server-test prompt

## Usage

This command invokes the `mcp-server-test` prompt using the prompt CLI.

```bash
prompt mcp-server-test
```

## Execution

When you invoke this command (e.g., `/mcp-server-test`), Claude Code will execute the full prompt workflow.

---

**Note**: This command is auto-generated from `~/git/dotfiles/prompts/mcp-server-test.md`. To update, modify the source prompt file and re-run `ax claude sync-prompts-to-commands`.

---

## Instructions

prompt mcp-server-test
