# create-issue

User-facing prompt to create high-quality GitHub issues with deep investigation and architecture alignment

## Usage

This command invokes the `create-issue` prompt using the prompt CLI.

```bash
prompt create-issue
```

## Execution

When you invoke this command (e.g., `/create-issue`), Claude Code will execute the full prompt workflow.

---

**Note**: This command is auto-generated from `~/git/dotfiles/prompts/create-issue.md`. To update, modify the source prompt file and re-run `ax claude sync-prompts-to-commands`.

---

## Instructions

prompt create-issue
