# dev-cycle

Execute development cycle - primary entry point for new and ongoing work

## Usage

This command invokes the `dev-cycle` prompt using the prompt CLI.

```bash
prompt dev-cycle
```

## Execution

When you invoke this command (e.g., `/dev-cycle`), Claude Code will execute the full prompt workflow.

---

**Note**: This command is auto-generated from `~/git/dotfiles/prompts/dev-cycle.md`. To update, modify the source prompt file and re-run `ax claude sync-prompts-to-commands`.

---

## Instructions

prompt dev-cycle
