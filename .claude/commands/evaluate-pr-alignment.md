# evaluate-pr-alignment

Evaluate PRs, issues, and their relationships for goal and architectural alignment

## Usage

This command invokes the `evaluate-pr-alignment` prompt using the prompt CLI.

```bash
prompt evaluate-pr-alignment
```

## Execution

When you invoke this command (e.g., `/evaluate-pr-alignment`), Claude Code will execute the full prompt workflow.

---

**Note**: This command is auto-generated from `~/git/dotfiles/prompts/evaluate-pr-alignment.md`. To update, modify the source prompt file and re-run `ax claude sync-prompts-to-commands`.

---

## Instructions

prompt evaluate-pr-alignment
