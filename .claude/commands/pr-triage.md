# pr-triage

Systematically analyze open PRs, assess alignment with architecture principles, and

## Usage

This command invokes the `pr-triage` prompt using the prompt CLI.

```bash
prompt pr-triage
```

## Execution

When you invoke this command (e.g., `/pr-triage`), Claude Code will execute the full prompt workflow.

---

**Note**: This command is auto-generated from `~/git/dotfiles/prompts/pr-triage.md`. To update, modify the source prompt file and re-run `ax claude sync-prompts-to-commands`.

---

## Instructions

prompt pr-triage
