# resume-cycle

Explicitly resume active workflow cycle from current phase

## Usage

This command invokes the `resume-cycle` prompt using the prompt CLI.

```bash
prompt resume-cycle
```

## Execution

When you invoke this command (e.g., `/resume-cycle`), Claude Code will execute the full prompt workflow.

---

**Note**: This command is auto-generated from `~/git/dotfiles/prompts/resume-cycle.md`. To update, modify the source prompt file and re-run `ax claude sync-prompts-to-commands`.

---

## Instructions

prompt resume-cycle
