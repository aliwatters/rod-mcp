# resolve-work-item

Execute resolve-work-item prompt

## Usage

This command invokes the `resolve-work-item` prompt using the prompt CLI.

```bash
prompt resolve-work-item
```

## Execution

When you invoke this command (e.g., `/resolve-work-item`), Claude Code will execute the full prompt workflow.

---

**Note**: This command is auto-generated from `~/git/dotfiles/prompts/resolve-work-item.md`. To update, modify the source prompt file and re-run `ax claude sync-prompts-to-commands`.

---

## Instructions

prompt resolve-work-item
