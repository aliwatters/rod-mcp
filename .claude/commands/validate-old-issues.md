# validate-old-issues

Execute validate-old-issues prompt

## Usage

This command invokes the `validate-old-issues` prompt using the prompt CLI.

```bash
prompt validate-old-issues
```

## Execution

When you invoke this command (e.g., `/validate-old-issues`), Claude Code will execute the full prompt workflow.

---

**Note**: This command is auto-generated from `~/git/dotfiles/prompts/validate-old-issues.md`. To update, modify the source prompt file and re-run `ax claude sync-prompts-to-commands`.

---

## Instructions

prompt validate-old-issues
