# groom-backlog

Interactively groom a selection of backlog items to ensure they are well-defined,

## Usage

This command invokes the `groom-backlog` prompt using the prompt CLI.

```bash
prompt groom-backlog
```

## Execution

When you invoke this command (e.g., `/groom-backlog`), Claude Code will execute the full prompt workflow.

---

**Note**: This command is auto-generated from `~/git/dotfiles/prompts/groom-backlog.md`. To update, modify the source prompt file and re-run `ax claude sync-prompts-to-commands`.

---

## Instructions

prompt groom-backlog
