# friction-analysis

Analyze workflow sessions to identify friction patterns, create improvements, and execute retrospectives

## Usage

This command invokes the `friction-analysis` prompt using the prompt CLI.

```bash
prompt friction-analysis
```

## Execution

When you invoke this command (e.g., `/friction-analysis`), Claude Code will execute the full prompt workflow.

---

**Note**: This command is auto-generated from `~/git/dotfiles/prompts/friction-analysis.md`. To update, modify the source prompt file and re-run `ax claude sync-prompts-to-commands`.

---

## Instructions

prompt friction-analysis
