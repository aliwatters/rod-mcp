# work-queue

Execute work-queue prompt

## Usage

This command invokes the `work-queue` prompt using the prompt CLI.

```bash
prompt work-queue
```

## Execution

When you invoke this command (e.g., `/work-queue`), Claude Code will execute the full prompt workflow.

---

**Note**: This command is auto-generated from `~/git/dotfiles/prompts/work-queue.md`. To update, modify the source prompt file and re-run `ax claude sync-prompts-to-commands`.

---

## Instructions

prompt work-queue
