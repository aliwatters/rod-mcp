# investigate-e2e-ci-failures

Execute investigate-e2e-ci-failures prompt

## Usage

This command invokes the `investigate-e2e-ci-failures` prompt using the prompt CLI.

```bash
prompt investigate-e2e-ci-failures
```

## Execution

When you invoke this command (e.g., `/investigate-e2e-ci-failures`), Claude Code will execute the full prompt workflow.

---

**Note**: This command is auto-generated from `~/git/dotfiles/prompts/investigate-e2e-ci-failures.md`. To update, modify the source prompt file and re-run `ax claude sync-prompts-to-commands`.

---

## Instructions

prompt investigate-e2e-ci-failures
