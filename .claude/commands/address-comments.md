# address-comments

Systematically address all PR review comments, including Copilot feedback, and post

## Usage

This command invokes the `address-comments` prompt using the prompt CLI.

```bash
prompt address-comments
```

## Execution

When you invoke this command (e.g., `/address-comments`), Claude Code will execute the full prompt workflow.

---

**Note**: This command is auto-generated from `~/git/dotfiles/prompts/address-comments.md`. To update, modify the source prompt file and re-run `ax claude sync-prompts-to-commands`.

---

## Instructions

prompt address-comments
