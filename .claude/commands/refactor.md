# refactor

Analyze codebase for refactoring opportunities and create improvement issues

## Usage

This command invokes the `refactor` prompt using the prompt CLI.

```bash
prompt refactor
```

## Execution

When you invoke this command (e.g., `/refactor`), Claude Code will execute the full prompt workflow.

---

**Note**: This command is auto-generated from `~/git/dotfiles/prompts/refactor.md`. To update, modify the source prompt file and re-run `ax claude sync-prompts-to-commands`.

---

## Instructions

prompt refactor
