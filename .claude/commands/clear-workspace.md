# clear-workspace

Reset workspace to clean state for starting fresh work

## Usage

This command invokes the `clear-workspace` prompt using the prompt CLI.

```bash
prompt clear-workspace
```

## Execution

When you invoke this command (e.g., `/clear-workspace`), Claude Code will execute the full prompt workflow.

---

**Note**: This command is auto-generated from `~/git/dotfiles/prompts/clear-workspace.md`. To update, modify the source prompt file and re-run `ax claude sync-prompts-to-commands`.

---

## Instructions

prompt clear-workspace
