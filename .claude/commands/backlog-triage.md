# backlog-triage

Continuously process ALL backlog issues and automatically triage them based on clear

## Usage

This command invokes the `backlog-triage` prompt using the prompt CLI.

```bash
prompt backlog-triage
```

## Execution

When you invoke this command (e.g., `/backlog-triage`), Claude Code will execute the full prompt workflow.

---

**Note**: This command is auto-generated from `~/git/dotfiles/prompts/backlog-triage.md`. To update, modify the source prompt file and re-run `ax claude sync-prompts-to-commands`.

---

## Instructions

prompt backlog-triage
