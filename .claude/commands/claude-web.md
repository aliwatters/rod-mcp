# claude-web

Package tasks for Claude Web environment and execute them via claude.ai/code interface

## Usage

This command invokes the `claude-web` prompt using the prompt CLI.

```bash
prompt claude-web
```

## Execution

When you invoke this command (e.g., `/claude-web`), Claude Code will execute the full prompt workflow.

---

**Note**: This command is auto-generated from `~/git/dotfiles/prompts/claude-web.md`. To update, modify the source prompt file and re-run `ax claude sync-prompts-to-commands`.

---

## Instructions

prompt claude-web
