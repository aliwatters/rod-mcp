# refine-issue

Interactive pre-flight validation of a single issue before starting work

## Usage

This command invokes the `refine-issue` prompt using the prompt CLI.

```bash
prompt refine-issue
```

## Execution

When you invoke this command (e.g., `/refine-issue`), Claude Code will execute the full prompt workflow.

---

**Note**: This command is auto-generated from `~/git/dotfiles/prompts/refine-issue.md`. To update, modify the source prompt file and re-run `ax claude sync-prompts-to-commands`.

---

## Instructions

prompt refine-issue
