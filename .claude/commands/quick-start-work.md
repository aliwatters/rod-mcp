# quick-start-work

Execute quick-start-work prompt

## Usage

This command invokes the `quick-start-work` prompt using the prompt CLI.

```bash
prompt quick-start-work
```

## Execution

When you invoke this command (e.g., `/quick-start-work`), Claude Code will execute the full prompt workflow.

---

**Note**: This command is auto-generated from `~/git/dotfiles/prompts/quick-start-work.md`. To update, modify the source prompt file and re-run `ax claude sync-prompts-to-commands`.

---

## Instructions

prompt quick-start-work
