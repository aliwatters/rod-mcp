# breakdown

Break down oversized issues into manageable ~8hr work items

## Usage

This command invokes the `breakdown` prompt using the prompt CLI.

```bash
prompt breakdown
```

## Execution

When you invoke this command (e.g., `/breakdown`), Claude Code will execute the full prompt workflow.

---

**Note**: This command is auto-generated from `~/git/dotfiles/prompts/breakdown.md`. To update, modify the source prompt file and re-run `ax claude sync-prompts-to-commands`.

---

## Instructions

prompt breakdown
