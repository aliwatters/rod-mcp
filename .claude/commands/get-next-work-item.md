# get-next-work-item

Navigate GitHub Project structures to find and resolve project-based work items with

## Usage

This command invokes the `get-next-work-item` prompt using the prompt CLI.

```bash
prompt get-next-work-item
```

## Execution

When you invoke this command (e.g., `/get-next-work-item`), Claude Code will execute the full prompt workflow.

---

**Note**: This command is auto-generated from `~/git/dotfiles/prompts/get-next-work-item.md`. To update, modify the source prompt file and re-run `ax claude sync-prompts-to-commands`.

---

## Instructions

prompt get-next-work-item
