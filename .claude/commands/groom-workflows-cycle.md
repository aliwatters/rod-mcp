# groom-workflows-cycle

Continuously maintain prompts and workflows - detect redundancy, enforce architecture

## Usage

This command invokes the `groom-workflows-cycle` prompt using the prompt CLI.

```bash
prompt groom-workflows-cycle
```

## Execution

When you invoke this command (e.g., `/groom-workflows-cycle`), Claude Code will execute the full prompt workflow.

---

**Note**: This command is auto-generated from `~/git/dotfiles/prompts/groom-workflows-cycle.md`. To update, modify the source prompt file and re-run `ax claude sync-prompts-to-commands`.

---

## Instructions

prompt groom-workflows-cycle
