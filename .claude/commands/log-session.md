# log-session

Capture all inputs and outputs from this AI agent session for deep analysis in another

## Usage

This command invokes the `log-session` prompt using the prompt CLI.

```bash
prompt log-session
```

## Execution

When you invoke this command (e.g., `/log-session`), Claude Code will execute the full prompt workflow.

---

**Note**: This command is auto-generated from `~/git/dotfiles/prompts/log-session.md`. To update, modify the source prompt file and re-run `ax claude sync-prompts-to-commands`.

---

## Instructions

prompt log-session
