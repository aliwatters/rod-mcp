# sentry

Fetch the most frequent Sentry issue, claim it, create a corresponding GitHub issue, and initiate the dev-cycle for investigation/fix.

## Usage

This command invokes the `sentry` prompt using the prompt CLI.

```bash
prompt sentry
```

## Execution

When you invoke this command (e.g., `/sentry`), Claude Code will execute the full prompt workflow.

---

**Note**: This command is auto-generated from `~/git/dotfiles/prompts/sentry.md`. To update, modify the source prompt file and re-run `ax claude sync-prompts-to-commands`.

---

## Instructions

prompt sentry
