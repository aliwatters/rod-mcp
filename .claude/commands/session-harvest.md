# session-harvest

Systematic post-workflow analysis to identify automation opportunities, broken tooling, and friction points

## Usage

This command invokes the `session-harvest` prompt using the prompt CLI.

```bash
prompt session-harvest
```

## Execution

When you invoke this command (e.g., `/session-harvest`), Claude Code will execute the full prompt workflow.

---

**Note**: This command is auto-generated from `~/git/dotfiles/prompts/session-harvest.md`. To update, modify the source prompt file and re-run `ax claude sync-prompts-to-commands`.

---

## Instructions

prompt session-harvest
