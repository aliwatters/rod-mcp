# project-cycle

Stateful workflow router for working on GitHub Project tasks.

## Usage

This command invokes the `project-cycle` prompt using the prompt CLI.

```bash
prompt project-cycle
```

## Execution

When you invoke this command (e.g., `/project-cycle`), Claude Code will execute the full prompt workflow.

---

**Note**: This command is auto-generated from `~/git/dotfiles/prompts/project-cycle.md`. To update, modify the source prompt file and re-run `ax claude sync-prompts-to-commands`.

---

## Instructions

prompt project-cycle
